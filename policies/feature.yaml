---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "feature"
  rules:
    - actions: ['*']
      effect: EFFECT_ALLOW
      roles:
        - admin

    - actions: ['read', 'list']
      effect: EFFECT_ALLOW
      roles:
        - user
