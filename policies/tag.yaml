---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "tag"
  rules:
    - actions: ['*']
      effect: EFFECT_ALLOW
      roles:
        - admin
        - editor

    - actions: ['list', 'read']
      effect: EFFECT_ALLOW
      roles:
        - user
        - viewer
